{
  "name": "sync-db-and-monday",
  "version": "0.1.0",
  "description": "Sync engine + management screen between Supabase DB and Monday.com, built on the existing control-plane.",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
